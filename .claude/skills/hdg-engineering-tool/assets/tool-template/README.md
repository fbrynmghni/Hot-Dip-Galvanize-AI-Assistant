# Template Tool Engineering

Salin ketiga file ke `apps/web/lib/tools/<nama-tool>/`, ganti `TOOL_NAME` dan
sesuaikan skema. Urutan kerja: isi `example.test.ts` lebih dulu (test gagal),
baru `index.ts`.
